# Edit Director - Cinematic Fastline

Read the complete `skills/pipelines/cinematic/edit-director.md` and
`skills/meta/fastline.md` before acting. Treat approved `final_props` and
`render_plan` as immutable. Write `edit_decisions` and a `change_impact` record;
any visual or timing change must route back to sample, while gain/LUFS-only
changes may remain on the validated mux path.

