# Executive Producer - Cinematic Fastline

Before executing this wrapper, read the complete
`skills/pipelines/cinematic/executive-producer.md` and
`skills/meta/fastline.md`. Keep its cinematic quality bar, but use the fastline
stage order and exactly two human stops: `creative_lock` at `assets`, then
`sample` approval. Resume from checkpoints and bundle state; do not regenerate
valid cached artifacts or bypass `video_compose`.

