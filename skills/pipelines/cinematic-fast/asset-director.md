# Asset Director - Cinematic Fastline

Read the complete `skills/pipelines/cinematic/asset-director.md` and
`skills/meta/fastline.md` before acting. Build `asset_plan`, `production_lock`
and the atomic `creative_lock` approval bundle. This first pass may inspect
cache availability and estimate cost, but must not call paid TTS, music or
generation providers and must not claim realized assets before approval.

